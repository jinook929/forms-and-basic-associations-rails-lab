<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.label :genre_id %>
  <!-- collection_select -->
  <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  <!-- select -->
  <%#= f.select :genre_id, Genre.all.collect {|g| [g.name, g.id]}, {prompt: 'Select Genre'} %>
  <!-- datalist -->
  <%#= f.text_field :genre_name, list: "genre_names", placeholder: "Enter Genre" %>
  <!--<datalist id="genre_names">-->
    <%# Genre.all.each do |genre| %>
      <!--<option value="<%#{}=genre.name%>"></option>-->
    <%# end %>    
  <!--</datalist>-->
  <br />

  <%= f.label :notes %>
  <input type="text" name="song[note_contents][]" id="song_notes_1">
  <input type="text" name="song[note_contents][]" id="song_notes_2">
  <input type="text" name="song[note_contents][]" id="song_notes_3">
  <br />

  <%= f.submit %>
<% end %>


